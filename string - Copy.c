#include<stdio.h>

int main(){

   char firstname[40];
   char fullname[50];
    scanf("%s",firstname);
    printf("the first name of user is :%s\n ",firstname);

    scanf("%s",fullname);
    printf("full name is %s",fullname);
 
    return 0;
}